
  # Healthcare Management System UI

  This is a code bundle for Healthcare Management System UI. The original project is available at https://www.figma.com/design/SRTTI2eH9CnnM2Y6mvUFtg/Healthcare-Management-System-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  